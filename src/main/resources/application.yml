server:
  port: ${SERVER_PORT:9001} # Default to 9001 if not set

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/investment_schema?useSSL=false&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:collo}
    password: ${SPRING_DATASOURCE_PASSWORD:Kipkirui92?}
    initialization-mode: always
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    serialization:
      fail-on-empty-beans: false

password:
  length: ${PASSWORD_LENGTH:8}

logging:
  level:
    org:
      hibernate: DEBUG
      hibernate.SQL: DEBUG

app:
  base-url: ${APP_BASE_URL:http://localhost:9001}
  email:
    retry:
      max-attempts: ${EMAIL_RETRY_MAX_ATTEMPTS:3}
      delay-hours: ${EMAIL_RETRY_DELAY_HOURS:1}
  jwt:
    secret: ${JWT_SECRET:CvsHphcFevtdY3HnphT7gUvnIgDin5hlQG+9O1cCD7vhGEltf+QsEasz1n91kqFCf8vkB4RDnf5nAfblMR+CgA==}
    token-validity: ${JWT_TOKEN_VALIDITY:3600000}
    refresh-token-validity: ${JWT_REFRESH_TOKEN_VALIDITY:2592000000}

file:
  upload-dir: ${FILE_UPLOAD_DIR:uploads/deposit-screenshots}
